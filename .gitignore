# Claude Code local settings — keep out of version control
.claude/

# Pi coding agent local state
.pi/

# Firebase CLI local deploy cache
.firebase/
.awareness/
.supervisor/

# Docker secrets — NEVER commit real values
secrets/*
!secrets/.gitkeep
